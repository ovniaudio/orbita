CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps

# CMake out-of-source build directory (artifacts — never commit)
/build/

**/.DS_Store

# It's nice to have the Builds folder exist, to remind us where it is
Builds/*
!Builds/.gitkeep

# This should never exist
Install/*

# Running CTest makes a bunch o junk
Testing

# IDE config
.idea

# clion cmake builds
cmake-build-debug
cmake-build-release
cmake-build-relwithdebinfo
packaging/Output/*build-release/
build-universal/
# cualquier build fuera de source (build-03, build-analisis, ...)
/build-*/

# Entregables del release (.pkg, bundles staged): pesan MB y se regeneran con
# packaging/make-orbit-pkg.sh. Nunca al repo publico.
/dist/
